version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    runtime-versions:
      python: 3.7
    #commands:
      # - command
  pre_build:
    commands:
      # - command
  build:
    commands:
      - echo Cats is the best movie ever
      #- cd ./introduction_to_codepipeline/labs
      #- coverage run --source . -m unittest discover && coverage report
  #post_build:
    #commands:
      # - command
      # - command
#artifacts:
  #files:
    # - location
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths